rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all entries
    // Note: This is appropriate for a public note-taking app
    // Add authentication later if you want to restrict access
    match /entries/{entry} {
      allow read, write: if true;
    }
  }
}
