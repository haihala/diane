# Environment Variables

# Firebase Configuration (Public - safe to expose in client-side code)
# Get these values from Firebase Console > Project Settings > General > Your apps

PUBLIC_FIREBASE_API_KEY=your-api-key-here
PUBLIC_FIREBASE_AUTH_DOMAIN=diane-prod.firebaseapp.com
PUBLIC_FIREBASE_PROJECT_ID=diane-prod

# Note: These are PUBLIC variables (prefixed with PUBLIC_)
# They are safe to expose in client-side code
# Firebase security is enforced through Firestore rules, not API keys
#
# This project uses:
# - Firestore (database)
# - Static file hosting
